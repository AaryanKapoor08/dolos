package com.dolos.transaction;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Smoke test: the Spring application context wires up and starts. */
@SpringBootTest
class TransactionServiceApplicationTests {

    @Test
    void contextLoads() {
        // Fails the build if the context cannot be created (bad config, missing beans, etc.).
    }
}
